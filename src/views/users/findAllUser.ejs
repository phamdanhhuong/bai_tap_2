<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh Sách Người Dùng</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border: none;
        }
        .table th {
            background-color: #007bff;
            color: white;
            border: none;
        }
        .btn-action {
            padding: 0.25rem 0.5rem;
            font-size: 0.875rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">Quản Lý Người Dùng</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/home">Trang Chủ</a>
                <a class="nav-link" href="/crud">CRUD</a>
                <a class="nav-link active" href="/get-crud">Danh Sách</a>
                <a class="nav-link" href="/about">Giới Thiệu</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h3 class="card-title mb-0">
                            <i class="fas fa-users"></i> Danh Sách Người Dùng
                        </h3>
                    </div>
                    <div class="card-body">
                        <% if (typeof datalist !== 'undefined' && datalist && datalist.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-striped table-hover">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Email</th>
                                            <th>Tên</th>
                                            <th>Họ</th>
                                            <th>Địa Chỉ</th>
                                            <th>SĐT</th>
                                            <th>Giới Tính</th>
                                            <th>Vai Trò</th>
                                            <th>Hành Động</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% datalist.forEach(function(user) { %>
                                        <tr>
                                            <td><%= user.id %></td>
                                            <td><%= user.email %></td>
                                            <td><%= user.firstName %></td>
                                            <td><%= user.lastName %></td>
                                            <td><%= user.address || 'N/A' %></td>
                                            <td><%= user.phoneNumber || 'N/A' %></td>
                                            <td>
                                                <% if (user.gender === true || user.gender === 1) { %>
                                                    <span class="badge bg-primary">Nam</span>
                                                <% } else { %>
                                                    <span class="badge bg-info">Nữ</span>
                                                <% } %>
                                            </td>
                                            <td><%= user.roleId || 'N/A' %></td>
                                            <td>
                                                <a href="/edit-crud?id=<%= user.id %>" 
                                                   class="btn btn-warning btn-action me-1">
                                                    <i class="fas fa-edit"></i> Sửa
                                                </a>
                                                <a href="/delete-crud?id=<%= user.id %>" 
                                                   class="btn btn-danger btn-action"
                                                   onclick="return confirm('Bạn có chắc chắn muốn xóa người dùng này?')">
                                                    <i class="fas fa-trash"></i> Xóa
                                                </a>
                                            </td>
                                        </tr>
                                        <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        <% } else { %>
                            <div class="text-center py-5">
                                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                                <h5 class="text-muted">Không có người dùng nào</h5>
                                <p class="text-muted">Hãy thêm người dùng mới để bắt đầu.</p>
                                <a href="/crud" class="btn btn-primary">
                                    <i class="fas fa-plus"></i> Thêm Người Dùng Mới
                                </a>
                            </div>
                        <% } %>
                    </div>
                    <div class="card-footer">
                        <div class="d-flex justify-content-between align-items-center">
                            <small class="text-muted">
                                Tổng số: <%= typeof datalist !== 'undefined' && datalist ? datalist.length : 0 %> người dùng
                            </small>
                            <a href="/crud" class="btn btn-success">
                                <i class="fas fa-plus"></i> Thêm Mới
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>
</html>
