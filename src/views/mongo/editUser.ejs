<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Người Dùng - MongoDB</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .card {
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            border: none;
            border-radius: 15px;
        }
        .form-label {
            font-weight: 600;
            color: #495057;
        }
        .required {
            color: #dc3545;
        }
        .mongo-green {
            background-color: #13aa52;
        }
        .readonly-field {
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark mongo-green">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="fas fa-leaf"></i> MongoDB User Management
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/mongo/home">Trang Chủ</a>
                <a class="nav-link" href="/mongo/crud">Tạo Mới</a>
                <a class="nav-link" href="/mongo/get-crud">Danh Sách</a>
                <a class="nav-link" href="/home">SQL Version</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-warning text-dark">
                        <h3 class="card-title mb-0">
                            <i class="fas fa-user-edit"></i> Chỉnh Sửa Người Dùng (MongoDB)
                        </h3>
                    </div>
                    <div class="card-body">
                        <% if (typeof error !== 'undefined' && error) { %>
                        <div class="alert alert-danger">
                            <i class="fas fa-exclamation-triangle"></i> <%= error %>
                        </div>
                        <% } %>

                        <% if (typeof data !== 'undefined' && data) { %>
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i> 
                            Chỉnh sửa thông tin người dùng trong MongoDB
                        </div>

                        <form action="/mongo/put-crud" method="POST">
                            <input type="hidden" name="id" value="<%= data._id %>">
                            
                            <!-- User ID Display -->
                            <div class="mb-3">
                                <label class="form-label">ID Người Dùng</label>
                                <input type="text" class="form-control readonly-field" 
                                       value="<%= data._id %>" readonly>
                                <small class="text-muted">MongoDB ObjectId - không thể thay đổi</small>
                            </div>
                            
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="firstName" class="form-label">
                                            Tên <span class="required">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="firstName" 
                                               name="firstName" value="<%= data.firstName %>" 
                                               required maxlength="50">
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="lastName" class="form-label">
                                            Họ <span class="required">*</span>
                                        </label>
                                        <input type="text" class="form-control" id="lastName" 
                                               name="lastName" value="<%= data.lastName %>" 
                                               required maxlength="50">
                                    </div>
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="email" class="form-label">
                                    Email <span class="required">*</span>
                                </label>
                                <input type="email" class="form-control readonly-field" id="email" 
                                       name="email" value="<%= data.email %>" readonly>
                                <small class="text-muted">Email không thể thay đổi để đảm bảo tính nhất quán</small>
                            </div>

                            <div class="mb-3">
                                <label for="address" class="form-label">Địa Chỉ</label>
                                <textarea class="form-control" id="address" name="address" 
                                          rows="3" placeholder="Nhập địa chỉ..." 
                                          maxlength="200"><%= data.address || '' %></textarea>
                                <div class="form-text">Tối đa 200 ký tự</div>
                            </div>

                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="phoneNumber" class="form-label">Số Điện Thoại</label>
                                        <input type="tel" class="form-control" id="phoneNumber" 
                                               name="phoneNumber" value="<%= data.phoneNumber || '' %>"
                                               placeholder="Nhập số điện thoại..."
                                               pattern="[\+]?[\d\s\-\(\)]{10,15}">
                                        <div class="form-text">Ví dụ: +84 123 456 789</div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="gender" class="form-label">Giới Tính</label>
                                        <select class="form-select" id="gender" name="gender">
                                            <option value="1" <%= (data.gender === true || data.gender === 1) ? 'selected' : '' %>>Nam</option>
                                            <option value="0" <%= (data.gender === false || data.gender === 0) ? 'selected' : '' %>>Nữ</option>
                                        </select>
                                    </div>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="roleId" class="form-label">Vai Trò</label>
                                        <select class="form-select" id="roleId" name="roleId">
                                            <option value="">Chọn vai trò...</option>
                                            <option value="R1" <%= data.roleId === 'R1' ? 'selected' : '' %>>
                                                Admin (Quản trị viên)
                                            </option>
                                            <option value="R2" <%= data.roleId === 'R2' ? 'selected' : '' %>>
                                                Doctor (Bác sĩ)
                                            </option>
                                            <option value="R3" <%= data.roleId === 'R3' ? 'selected' : '' %>>
                                                Patient (Bệnh nhân)
                                            </option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="positionId" class="form-label">Vị Trí</label>
                                        <input type="text" class="form-control" id="positionId" 
                                               name="positionId" value="<%= data.positionId || '' %>"
                                               placeholder="Nhập vị trí công việc...">
                                    </div>
                                </div>
                            </div>

                            <!-- Metadata -->
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label class="form-label">Ngày Tạo</label>
                                        <input type="text" class="form-control readonly-field" 
                                               value="<%= data.createdAt ? new Date(data.createdAt).toLocaleString('vi-VN') : 'N/A' %>" 
                                               readonly>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label class="form-label">Cập Nhật Lần Cuối</label>
                                        <input type="text" class="form-control readonly-field" 
                                               value="<%= data.updatedAt ? new Date(data.updatedAt).toLocaleString('vi-VN') : 'N/A' %>" 
                                               readonly>
                                    </div>
                                </div>
                            </div>

                            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                                <a href="/mongo/get-crud" class="btn btn-secondary me-md-2">
                                    <i class="fas fa-arrow-left"></i> Quay Lại
                                </a>
                                <button type="submit" class="btn btn-warning">
                                    <i class="fas fa-save"></i> Cập Nhật
                                </button>
                            </div>
                        </form>
                        <% } else { %>
                        <div class="text-center py-5">
                            <i class="fas fa-exclamation-triangle fa-3x text-warning mb-3"></i>
                            <h5 class="text-muted">Không tìm thấy thông tin người dùng</h5>
                            <p class="text-muted">
                                Người dùng có thể đã bị xóa hoặc ID không hợp lệ.
                            </p>
                            <a href="/mongo/get-crud" class="btn btn-primary">
                                <i class="fas fa-arrow-left"></i> Quay Lại Danh Sách
                            </a>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/js/all.min.js"></script>
</body>
</html>
